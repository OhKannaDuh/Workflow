using Ocelot.Modules;

namespace Workflow.Modules.{{name}};

[OcelotModule]
public class {{name}}Module(Plugin plugin, Config config) : Module(plugin, config)
{
    public override OcelotConfig Config {
        get => PluginConfig.{{name}}Config;
    }
}
